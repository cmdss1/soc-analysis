# SOC Sandbox stack — compose hints only (`docker compose up`).
#
# Use MOCK_KASM=true for UI/collector testing without a Kasm deployment.
# For production-like runs: disable MOCK_* and set all KASM_* variables plus an
# IP/DNS hostname for PUBLIC_API_BASE that Kasm workspace containers can reach.
